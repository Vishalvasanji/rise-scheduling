diff --git a/node_modules/gantt-task-react/dist/index.js b/node_modules/gantt-task-react/dist/index.js
index 626bb39..b5d0072 100644
--- a/node_modules/gantt-task-react/dist/index.js
+++ b/node_modules/gantt-task-react/dist/index.js
@@ -133,7 +133,7 @@ var ganttDateRange = function ganttDateRange(tasks, viewMode, preStepsCount) {
 
     case exports.ViewMode.Week:
       newStartDate = startOfDate(newStartDate, "day");
-      newStartDate = addToDate(getMonday(newStartDate), -7 * preStepsCount, "day");
+      newStartDate = addToDate(getSunday(newStartDate), -7 * preStepsCount, "day");
       newEndDate = startOfDate(newEndDate, "day");
       newEndDate = addToDate(newEndDate, 1.5, "month");
       break;
@@ -224,9 +224,9 @@ var getLocalDayOfWeek = function getLocalDayOfWeek(date, locale, format) {
   return bottomValue;
 };
 
-var getMonday = function getMonday(date) {
+var getSunday = function getSunday(date) {
   var day = date.getDay();
-  var diff = date.getDate() - day + (day === 0 ? -6 : 1);
+  var diff = date.getDate() - day;
   return new Date(date.setDate(diff));
 };
 
@@ -638,8 +638,11 @@ var GridBody = function GridBody(_ref) {
     }));
 
     if (i + 1 !== dates.length && date.getTime() < now.getTime() && dates[i + 1].getTime() >= now.getTime() || i !== 0 && i + 1 === dates.length && date.getTime() < now.getTime() && addToDate(date, date.getTime() - dates[i - 1].getTime(), "millisecond").getTime() >= now.getTime()) {
+      var todayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate());
+      var todayNext = i + 1 < dates.length ? dates[i + 1].getTime() : addToDate(date, date.getTime() - dates[i - 1].getTime(), "millisecond").getTime();
+      var todayFrac = Math.min(1, Math.max(0, (todayStart.getTime() - date.getTime()) / (todayNext - date.getTime())));
       today = React__default.createElement("rect", {
-        x: tickX,
+        x: tickX + columnWidth * todayFrac,
         y: 0,
         width: columnWidth,
         height: y,
@@ -762,9 +765,11 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dateSetup.dates.length; i++) {
       var date = dateSetup.dates[i];
-      var bottomValue = getLocaleMonth(date, locale);
+      var bottomValue = getCachedDateTimeFormat(locale, {
+        month: "short"
+      }).format(date);
       bottomValues.push(React__default.createElement("text", {
-        key: bottomValue + date.getFullYear(),
+        key: date.getMonth() + "-" + date.getFullYear(),
         y: headerHeight * 0.8,
         x: columnWidth * i + columnWidth * 0.5,
         className: styles$5.calendarBottomText
@@ -810,7 +815,7 @@ var Calendar = function Calendar(_ref) {
         topValue = getLocaleMonth(date, locale) + ", " + date.getFullYear();
       }
 
-      var bottomValue = "W" + getWeekNumberISO8601(date);
+      var bottomValue = date.getDate().toString();
       bottomValues.push(React__default.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
@@ -848,7 +853,7 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dates.length; i++) {
       var date = dates[i];
-      var bottomValue = getLocalDayOfWeek(date, locale, "short") + ", " + date.getDate().toString();
+      var bottomValue = date.getDate().toString();
       bottomValues.push(React__default.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
diff --git a/node_modules/gantt-task-react/dist/index.modern.js b/node_modules/gantt-task-react/dist/index.modern.js
index b372269..9f00bf8 100644
--- a/node_modules/gantt-task-react/dist/index.modern.js
+++ b/node_modules/gantt-task-react/dist/index.modern.js
@@ -132,7 +132,7 @@ var ganttDateRange = function ganttDateRange(tasks, viewMode, preStepsCount) {
 
     case ViewMode.Week:
       newStartDate = startOfDate(newStartDate, "day");
-      newStartDate = addToDate(getMonday(newStartDate), -7 * preStepsCount, "day");
+      newStartDate = addToDate(getSunday(newStartDate), -7 * preStepsCount, "day");
       newEndDate = startOfDate(newEndDate, "day");
       newEndDate = addToDate(newEndDate, 1.5, "month");
       break;
@@ -223,9 +223,9 @@ var getLocalDayOfWeek = function getLocalDayOfWeek(date, locale, format) {
   return bottomValue;
 };
 
-var getMonday = function getMonday(date) {
+var getSunday = function getSunday(date) {
   var day = date.getDay();
-  var diff = date.getDate() - day + (day === 0 ? -6 : 1);
+  var diff = date.getDate() - day;
   return new Date(date.setDate(diff));
 };
 
@@ -637,8 +637,11 @@ var GridBody = function GridBody(_ref) {
     }));
 
     if (i + 1 !== dates.length && date.getTime() < now.getTime() && dates[i + 1].getTime() >= now.getTime() || i !== 0 && i + 1 === dates.length && date.getTime() < now.getTime() && addToDate(date, date.getTime() - dates[i - 1].getTime(), "millisecond").getTime() >= now.getTime()) {
+      var todayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate());
+      var todayNext = i + 1 < dates.length ? dates[i + 1].getTime() : addToDate(date, date.getTime() - dates[i - 1].getTime(), "millisecond").getTime();
+      var todayFrac = Math.min(1, Math.max(0, (todayStart.getTime() - date.getTime()) / (todayNext - date.getTime())));
       today = React.createElement("rect", {
-        x: tickX,
+        x: tickX + columnWidth * todayFrac,
         y: 0,
         width: columnWidth,
         height: y,
@@ -761,9 +764,11 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dateSetup.dates.length; i++) {
       var date = dateSetup.dates[i];
-      var bottomValue = getLocaleMonth(date, locale);
+      var bottomValue = getCachedDateTimeFormat(locale, {
+        month: "short"
+      }).format(date);
       bottomValues.push(React.createElement("text", {
-        key: bottomValue + date.getFullYear(),
+        key: date.getMonth() + "-" + date.getFullYear(),
         y: headerHeight * 0.8,
         x: columnWidth * i + columnWidth * 0.5,
         className: styles$5.calendarBottomText
@@ -809,7 +814,7 @@ var Calendar = function Calendar(_ref) {
         topValue = getLocaleMonth(date, locale) + ", " + date.getFullYear();
       }
 
-      var bottomValue = "W" + getWeekNumberISO8601(date);
+      var bottomValue = date.getDate().toString();
       bottomValues.push(React.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
@@ -847,7 +852,7 @@ var Calendar = function Calendar(_ref) {
 
     for (var i = 0; i < dates.length; i++) {
       var date = dates[i];
-      var bottomValue = getLocalDayOfWeek(date, locale, "short") + ", " + date.getDate().toString();
+      var bottomValue = date.getDate().toString();
       bottomValues.push(React.createElement("text", {
         key: date.getTime(),
         y: headerHeight * 0.8,
